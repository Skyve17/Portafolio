version: '3.8'

services:
  portafolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portafolio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
